<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="app.goalkickr.com" />
                <data android:scheme="https" android:host="goalkickr.app" />
                <data android:scheme="https" android:host="firefc.app" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>

        <!-- Player widget (Android counterpart of ios/App/PlayerWidget).
             Receiver listens for AppWidgetManager updates; the configure
             activity runs when the user drops the widget on the home
             screen and asks for the setup code. updatePeriodMillis is
             set in player_widget_info.xml. -->
        <receiver
            android:name=".widget.PlayerWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/player_widget_info" />
        </receiver>

        <activity
            android:name=".widget.PlayerWidgetConfigure"
            android:exported="true"
            android:theme="@android:style/Theme.DeviceDefault.NoActionBar">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Push notifications (FCM) — required on Android 13+ (API 33+);
         ignored on older versions. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- 2026-07-14: CAMERA / READ_MEDIA_IMAGES / READ_MEDIA_VIDEO
         removed. @capacitor/camera is in package.json but never
         imported at any real callsite; every media upload path
         uses HTML <input type="file"> against R2/Stream, which on
         modern Android goes through the system photo picker and
         does NOT require these permissions. Declaring them triggers
         Play Console data-safety questionnaires we can't honestly
         answer. Re-add here + drop the plugin whenever a native
         camera path (barcode scan, in-app capture UI, etc.) ships. -->
</manifest>
